<%= form_with model: :comment, url: user_post_comments_path(user_id: @user.id, post_id: @post.id), method: :post, local: true, scope: :comment do |f| %>
  <div>
    <div>
      <%= f.text_area :text %>
    </div>
    <div>
      <%= f.submit 'Comment' %>
    </div>
  </div>
<% end %>